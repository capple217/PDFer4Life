import { TextEditor } from "text-editor.slint";
import {PDFRender} from "pdf-render.slint";
import {PDFerPalette} from "common.slint";

export component SplitPage inherits Rectangle {
    background: PDFerPalette.background;
    border-color: black;
    width: 100%;
    height: 100%;
    HorizontalLayout {
        PDFRender { }
        TextEditor { }
    }
}
