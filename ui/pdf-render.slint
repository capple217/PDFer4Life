// import { } from "std-widgets.slint";
import { BackendPDF } from "common.slint";

export component PDFRender inherits Window {
    Rectangle { border-color: yellow; min-width: 20px;border-width: 2px; }
}