import { TextEdit, Button } from "std-widgets.slint";
import { BackendTextEditor } from "global-callbacks.slint";
import {Page} from "common.slint";

export component TextEditor inherits Page {
    out property <string> notes-text

    VerticalLayout {
        TextEdit {
            font-size: 14px;
            height: 80%;
            width: 50%;
            text: "This is editable text";
            edited => {

            }
        }

        // Buttons for saving and loading files
        VerticalLayout {
            Button {
                text: "Save";
                clicked => {
                    BackendTextEditor.save-file("notes.txt", "this is my text that I inputted");
                }
            }
            Button {
                text: "Load";
                //clicked => load_clicked();
            }
        }

        TouchArea {
            moved => {
                self.width = self.mouse-x;
            }
        }
    }
}